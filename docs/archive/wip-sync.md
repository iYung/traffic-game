## Wip-Sync Checklist

- [x] Task 1 — `core/lua/scene_manager.lua` — Replace the hard-cut SceneManager with the fade-transition version from `wip/lua/core/scene_manager.lua`. Key changes: add `FADE_DURATION = 0.3` constant; add `_prev`, `_fade_state`, `_fade_alpha` fields to `SceneManager.new()`; change `switch()` so the first load (no current scene) skips the fade while subsequent calls store the old scene in `_prev`, call `on_enter()` on the new scene immediately, and set `_fade_state = "out"`; update `update(dt)` to drive alpha ramp-up ("out") then ramp-down ("in"), calling `_prev:on_exit()` at peak opacity; update `draw()` to render `_prev` during fade-out and overlay a black rectangle at `_fade_alpha` opacity. **Adapt**: replace `config.LOGICAL_W/H` (wip uses `require("lua/game/config")`) with constructor args `SceneManager.new(w, h)` defaulting to `1280, 720` — store as `self._W` and `self._H`. Update the `love.graphics.rectangle` call in `draw()` to use `self._W, self._H`.

- [x] Task 2 — `core/lua/fonts.lua` — Create this new file as a direct port of `wip/lua/core/fonts.lua` (no changes needed beyond the file path). The file exposes a single `from(path, hinting)` function returning a factory object with a `new(size)` method that calls `love.graphics.newFont(path, size, hinting)`. No game-specific paths or bindings; callers supply the font path.

- [x] Task 3 — `conf.lua` — Create this new file at the project root (love-exemplar has no `conf.lua`). Port from `wip/conf.lua` with these changes: set `t.window.title = "Love Exemplar"` instead of the wip game title; keep `t.window.width = 1280`, `t.window.height = 720`, `t.window.resizable = true`; keep the `--headless` arg detection block that disables `window`, `graphics`, `audio`, `sound`, `joystick`, `touch`, and `video` modules.

- [x] Task 4 — `assets/images/icon.png` — Add a placeholder 32×32 icon PNG at this path. The file does not exist yet (`assets/` only contains `player.png`). Generate a minimal valid PNG (e.g., a solid-color 32×32 image) using Python: `python3 -c "import struct, zlib; ..."` or copy an existing PNG and resize. The file must be a valid PNG so `love.image.newImageData("assets/images/icon.png")` succeeds at runtime.

- [x] Task 5 — `lua/headless/stubs.lua` — Create this new file as a direct port of `wip/lua/headless/stubs.lua`. Remove the last line (`package.loaded["lua/game/assets"] = nil`) — that line busts the wip game's asset cache and has no equivalent in the exemplar. All other content is generic: the `graphics_stub` table with explicit no-ops, the `new*` catch-all metatable, the stub image factory (`getWidth/getHeight/getDimensions/setFilter`), `love.keyboard.isDown`, `love.window`, `love.filesystem`, `love.audio`, and `love.audio.newSource` stubs.

- [x] Task 6 — `lua/headless/input.lua` — Create this new file as a verbatim port of `wip/lua/headless/input.lua`. No path or API changes required: the `HeadlessInput` class (`press`, `hold`, `release`, `update`, `is_down`, `pressed`) is fully generic and matches the `core/lua/input.lua` interface used by `game/player.lua`.

- [x] Task 7 — `lua/headless/runner.lua` — Create this new file adapted from `wip/lua/headless/runner.lua`. Changes required: remove the `GameState` and `StartScene` imports (lines 1 and 4 of the wip file); remove the `local gs = GameState.new()` line inside `runner.setup()`; remove the `else` branch that calls `StartScene.new(gs, input, sm)` — instead make `scene_factory` a required argument (error if nil). Change `require("lua/core/scene_manager")` to `require("core/lua/scene_manager")` and `require("lua/headless/input")` to `lua/headless/input` (paths are the same since both repos use the same `lua/headless/` layout for this new dir). Update `SceneManager.new()` call to pass logical width/height args: `SceneManager.new(1280, 720)`. Keep `runner._visual`, `runner._active_sm`, `runner.tick`, `runner.fast_forward_until`, and `runner.run` unchanged. `runner.setup(scene_factory)` returns `{ input = input, sm = sm }` (no `gs` since GameState is no longer created here).

- [x] Task 8 — `main.lua` — Update the existing `main.lua` to add canvas rendering, pixel-art filter, headless/visual mode plumbing, and window icon. Specific changes: (a) Add headless/visual arg-parsing block at the top (before any requires): detect `--headless` and `--visual` flags; on `--headless` require `lua/headless/stubs` then call `require("lua/headless/runner").run(test_file)` and `return`; store `_visual_mode` and `_visual_test` for visual mode. (b) Add `love.graphics.setDefaultFilter("nearest", "nearest")` after the arg-parsing block. (c) Keep existing `SceneManager` and `GameScene` requires. (d) Add `local LOGICAL_W, LOGICAL_H = 1280, 720` and `local canvas` locals. (e) In `love.load()`: add `love.window.setIcon(love.image.newImageData("assets/images/icon.png"))` before existing code; create `canvas = love.graphics.newCanvas(LOGICAL_W, LOGICAL_H); canvas:setFilter("nearest", "nearest")`; change `SceneManager.new()` to `SceneManager.new(LOGICAL_W, LOGICAL_H)`. (f) Replace `love.draw()` body with: set canvas, clear, call `manager:draw()`, unset canvas, compute letterbox scale/offset, draw canvas to screen. (g) Keep `love.update`, `love.keypressed` unchanged. Do NOT add game-specific modules (Sound, Save, SettingsMenu, GameState, StartScene) — exemplar stays minimal.

- [x] Task 9 — `tests/test_basics.lua` — Create this new file demonstrating the headless test infrastructure against the exemplar's own `GameScene`. The test should: (1) require `lua/headless/runner`; (2) set up a context with `runner.setup(function(input, sm) return require("game/scenes/game_scene").new() end)` — note the adapted scene_factory signature matches Task 7's simplified runner (no `gs` arg); (3) assert that a fresh scene can be ticked without error (`runner.tick(ctx.input, ctx.sm, 10)`); (4) test that the player's centre x advances when the `"right"` action is held for 30 frames — but note `game/player.lua` uses its own internal `Input` instance, so the exemplar test should assert on something observable without a shared input handle. A safe alternative: assert that `ctx.sm.current ~= nil` after tick, and add a comment explaining the player's internal input prevents headless action injection. Print `PASS: scene ticks without error` and similar markers. Keep the test simple — its purpose is to show the infrastructure works, not to cover gameplay logic.
